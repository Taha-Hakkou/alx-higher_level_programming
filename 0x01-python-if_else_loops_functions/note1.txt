* resources

* man python3

* importance of indentation

* if/elif/else statements
	if condition1:
		// instructions_1
	elif condition2:
		// instructions_2
	...
	else:
		// instructions_n

* and, or, not, is keywords

* True, False keywords

* comments

* while / for loops
	for element in elements:
		// instructions

* for (python vs c)

* 'break' & 'continues' statements

* 'else' clauses on loops

* 'pass' statement

* range
	range(a)		-> from 0 to a-1 (1 step)
	range(a,b)		-> from a to b-1 (1 step)
	range(a,b,c)	-> from a to(excluded) with c steps

* functions in c
	what does return w/ no 'return' statement

* scope of vars

* traceback ?

* arithmetic operators in python

* 'end' param in print function

* == operator

* import random
	random.randint(a,b)	-> random signed number between a & b

* ord(), chr(), hex(), 

* display a leading zero for all numbers with less than two digits: {i:02d}

* str.upper()  |  str.isupper()

* #!/usr/bin/env python3 ==? #!/usr/bin/python3

* when working in python project (many files):
	func = __import__('filename_without_extension').func

* print("a is {}".format("lower" if islower("a") else "upper"))

* if i in range():

* abs(), len()

* '**' operator
